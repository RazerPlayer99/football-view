# Football View (Pre-Alpha) Configuration
#
# API-Football Configuration
# Get a free API key at: https://www.api-football.com/
API_FOOTBALL_KEY=your_api_key_here

# LLM Configuration (optional - improves search when rule-based uncertain)
# Get an API key at: https://console.anthropic.com/
# Without this key, search uses rule-based classification only
ANTHROPIC_API_KEY=

# Cache Settings
CACHE_ENABLED=true
CACHE_DIRECTORY=./cache
CACHE_TTL_SECONDS=3600

# Output Settings
OUTPUT_DIRECTORY=./output

# Rate Limiting
REQUESTS_PER_MINUTE=30

# Search Logging (set to 1 to enable query logging for debugging)
SEARCH_LOGGING=0

# Season Override (defaults to 2024 for 2024-25 season)
# CURRENT_SEASON=2024
